# 📝 Shannon's Blog 使用指南

这是一份详细的博客使用和维护指南，包含文章发布、图片管理、Cloudflare R2 配置等完整流程。

## 📋 目录

1. [项目结构](#项目结构)
2. [环境配置](#环境配置)
3. [发布文章](#发布文章)
4. [图片管理](#图片管理)
5. [Cloudflare R2 配置](#cloudflare-r2-配置)
6. [部署流程](#部署流程)
7. [常见问题](#常见问题)

## 📁 项目结构

```
blog/
├── posts/                    # 文章目录（重要）
│   └── dev/                 # 分类目录（可自定义）
│       └── article.mdx      # 文章文件
├── public/                  # 静态资源目录
│   ├── images/             # 临时图片存放
│   ├── covers/             # 封面图片
│   ├── avatar.jpg          # 头像
│   └── favicon.ico         # 网站图标
├── scripts/                # 自动化脚本
│   ├── uploadMedia.ts      # 媒体上传脚本
│   └── config.ts           # 配置文件
└── src/                    # 源代码
```

## ⚙️ 环境配置

### 1. 创建环境变量文件

在项目根目录创建 `.env` 文件：

```bash
# 在项目根目录执行
touch .env
```

### 2. 配置环境变量

编辑 `.env` 文件，添加以下内容：

```env
# Cloudflare 账户 ID
CLOUDFLARE_ACCOUNT_ID=your_account_id

# R2 存储桶配置
R2_ACCESS_KEY_ID=your_access_key
R2_SECRET_ACCESS_KEY=your_secret_key
R2_BUCKET_NAME=your_bucket_name
R2_PUBLIC_URL=https://your-domain.com

# 网站配置（可选）
NEXT_PUBLIC_WEBSITE_URL=https://shannonwang.top
```

### 3. 安装依赖

```bash
# 使用 pnpm（推荐）
pnpm install

# 或使用 npm
npm install
```

## ✍️ 发布文章

### 1. 创建文章文件

在 `posts/` 目录下创建分类文件夹和文章：

```bash
# 示例：创建开发类文章
mkdir -p posts/dev
touch posts/dev/20240315_my_new_article.mdx
```

### 2. 文章格式模板

```markdown
---
title: 文章标题
description: 文章简介描述
date: 2024-03-15
category: dev
tags: Next.js, React, TypeScript
cover: /covers/article-cover.jpg
featured: false
slug: my-custom-url-slug
---

## 文章正文

这里是文章内容...

### 插入图片

![图片描述](/images/example.jpg)

### 插入代码

```typescript
const example = "Hello World";
console.log(example);
```

### 插入视频

可以使用 HTML 标签插入视频：

```html
<video src="/videos/demo.mp4" controls></video>
```
```

### 3. 文章字段说明

| 字段 | 类型 | 必需 | 说明 |
|------|------|------|------|
| `title` | string | ✅ | 文章标题 |
| `description` | string | ❌ | 文章描述（SEO 用） |
| `date` | string | ✅ | 发布日期（YYYY-MM-DD） |
| `category` | string | ✅ | 文章分类 |
| `tags` | string | ❌ | 标签（逗号分隔） |
| `cover` | string | ❌ | 封面图片路径 |
| `featured` | boolean | ❌ | 是否为特色文章 |
| `slug` | string | ❌ | 自定义 URL 路径 |

### 4. 分类系统

支持的分类目录（可自定义）：
- `dev/` - 开发技术
- `ai/` - 人工智能
- `product/` - 产品设计
- `life/` - 生活随笔

## 🖼️ 图片管理

### 1. 添加图片的完整流程

#### 步骤 1：放置图片到临时目录
```bash
# 将图片放入 public 目录的相应文件夹
# 封面图片
cp your-cover.jpg public/covers/

# 文章内图片
cp your-image.jpg public/images/
```

#### 步骤 2：在文章中引用图片
```markdown
---
title: 我的文章
cover: /covers/your-cover.jpg
---

文章内容...

![图片描述](/images/your-image.jpg)
```

#### 步骤 3：运行媒体上传脚本
```bash
# 测试模式（推荐先测试）
pnpm upload-media --dry-run

# 正式上传
pnpm upload-media
```

### 2. 脚本会自动处理

✅ **自动上传** 图片到 Cloudflare R2  
✅ **自动更新** MDX 文件中的图片链接  
✅ **自动删除** 本地图片文件  
✅ **自动清理** 未使用的图片  
✅ **自动备份** 原始文件  

### 3. 上传后的效果

```markdown
# 之前
cover: /covers/your-cover.jpg
![图片](/images/your-image.jpg)

# 之后
cover: https://media.shannonotes.com/covers/your-cover.jpg
![图片](https://media.shannonotes.com/images/your-image.jpg)
```

### 4. 脚本命令选项

```bash
# 基本用法
pnpm upload-media                    # 正常运行
pnpm upload-media --dry-run          # 测试模式，不实际修改文件
pnpm upload-media --skip-backup      # 跳过文件备份
pnpm upload-media --skip-cleanup     # 跳过清理未使用的文件

# 获取帮助
pnpm upload-media --help
```

## ☁️ Cloudflare R2 配置

### 1. 创建 R2 存储桶

1. 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
2. 进入 **R2 Object Storage**
3. 点击 **"Create bucket"**
4. 输入存储桶名称（如：`my-blog-media`）
5. 选择区域（推荐：**亚太地区**）

### 2. 获取 API 密钥

#### 步骤 1：创建 API Token
1. 进入 **"Manage R2 API tokens"**
2. 点击 **"Create API token"**
3. 设置权限：
   - **Permissions**: `Admin`
   - **Resources**: `Include - All accounts`

#### 步骤 2：获取账户信息
```bash
# 获取账户 ID
# 在 Cloudflare Dashboard 右侧面板可以找到 Account ID
```

### 3. 配置自定义域名（可选）

#### 步骤 1：添加自定义域名
1. 在 R2 存储桶设置中选择 **"Custom domains"**
2. 点击 **"Connect domain"**
3. 输入域名（如：`media.shannonotes.com`）

#### 步骤 2：更新 DNS 记录
```dns
# 添加 CNAME 记录
Type: CNAME
Name: media
Target: [Cloudflare 提供的目标地址]
```

#### 步骤 3：更新环境变量
```env
R2_PUBLIC_URL=https://media.shannonotes.com
```

### 4. 存储桶权限设置

确保存储桶允许公开读取：

1. 进入存储桶设置
2. 选择 **"Settings"** → **"Public access"**
3. 启用 **"Allow public read access"**

## 🚀 部署流程

### 1. 本地开发

```bash
# 启动开发服务器
pnpm dev

# 访问 http://localhost:3000
```

### 2. 发布文章的完整流程

```bash
# 1. 创建新文章
touch posts/dev/my-article.mdx

# 2. 编写文章内容（参考上面的模板）

# 3. 添加图片到 public 目录

# 4. 测试媒体上传
pnpm upload-media --dry-run

# 5. 正式上传媒体
pnpm upload-media

# 6. 本地测试
pnpm dev

# 7. 构建检查
pnpm build

# 8. 提交代码
git add .
git commit -m "新增文章：文章标题"
git push origin main

# 9. Vercel 会自动部署
```

### 3. Vercel 环境变量配置

在 Vercel Dashboard 中配置生产环境变量：

1. 进入项目设置
2. 选择 **"Environment Variables"**
3. 添加以下变量：
   ```
   CLOUDFLARE_ACCOUNT_ID
   R2_ACCESS_KEY_ID
   R2_SECRET_ACCESS_KEY
   R2_BUCKET_NAME
   R2_PUBLIC_URL
   ```

## 🔧 常见问题

### Q1: 上传脚本报错 "请在 .env 文件中设置所有必要的环境变量"

**解决方案：**
```bash
# 检查 .env 文件是否存在
ls -la .env

# 检查环境变量是否正确设置
cat .env

# 确保包含所有必需的变量：
# CLOUDFLARE_ACCOUNT_ID
# R2_ACCESS_KEY_ID
# R2_SECRET_ACCESS_KEY
# R2_BUCKET_NAME
# R2_PUBLIC_URL
```

### Q2: 图片上传失败

**可能原因：**
1. R2 API 密钥错误
2. 存储桶权限设置错误
3. 网络连接问题

**解决方案：**
```bash
# 1. 测试模式检查
pnpm upload-media --dry-run

# 2. 检查 R2 设置
# - 确认存储桶名称正确
# - 确认 API 密钥有效
# - 确认存储桶有写入权限

# 3. 单独测试小文件
```

### Q3: 本地开发时图片显示不出来

**解决方案：**
```bash
# 1. 确保图片放在 public 目录下
# 2. 检查图片路径（以 / 开头）
# 3. 重启开发服务器
pnpm dev
```

### Q4: 部署后图片链接失效

**可能原因：**
1. 忘记运行上传脚本
2. CDN 域名配置错误

**解决方案：**
```bash
# 1. 重新运行上传脚本
pnpm upload-media

# 2. 检查 R2_PUBLIC_URL 是否正确

# 3. 重新部署
git add .
git commit -m "更新媒体链接"
git push origin main
```

### Q5: 如何批量处理历史文章的图片

**解决方案：**
```bash
# 1. 将所有历史图片放入 public 目录对应位置

# 2. 运行脚本处理所有 MDX 文件
pnpm upload-media

# 3. 脚本会自动：
#    - 扫描所有 MDX 文件
#    - 上传引用的图片
#    - 更新文件中的链接
```

## 📞 获取帮助

如果遇到问题：

1. **查看日志**：运行脚本时注意错误信息
2. **测试模式**：使用 `--dry-run` 先测试
3. **备份恢复**：脚本会自动备份，如需恢复查看 `backups/` 目录
4. **重新初始化**：删除 `.env` 重新配置

## 🎯 最佳实践

### 1. 文章命名规范
- 使用日期前缀：`20240315_article_title.mdx`
- 使用英文文件名，避免特殊字符
- 保持简短且描述性强

### 2. 图片优化建议
- 使用 WebP 格式（体积更小）
- 控制图片尺寸（建议宽度不超过 1200px）
- 压缩图片（保持质量 80-90%）

### 3. SEO 优化
- 填写准确的 `description`
- 使用相关的 `tags`
- 设置合适的 `cover` 图片

### 4. 内容组织
- 合理使用分类目录
- 保持文章结构清晰
- 添加目录和小标题

---

💡 **提示**: 这份指南涵盖了博客的完整使用流程。建议先在测试模式下熟悉各项操作，然后再进行正式的内容发布。